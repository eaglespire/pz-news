class Article {
  String? title;
  String? description;
  String? url;
  String? urlToImage;
  String? source;

  Article(
      {this.description, this.title, this.source, this.url, this.urlToImage});
}
